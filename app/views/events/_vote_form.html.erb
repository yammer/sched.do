<td>
  <div data-id="<%= suggestion.id %>">
    <%= semantic_form_for form_object, remote: true, html: input do |form| %>
      <%= form.input :id, as: :hidden, input_html: vote_id  %>
      <%= form.input :event_id, as: :hidden,
        input_html: { value: suggestion.event.id } %>
      <%= form.input :suggestion_id, as: :hidden,
        input_html: { value: suggestion.id }%>
      <%= form.input :suggestion_type, as: :hidden,
        input_html: { value: suggestion.class.name }%>
      <%= form.action :submit,
        button_html: button.merge(disabled: suggestion.event.closed?) %>
    <% end %>
  </div>
</td>
