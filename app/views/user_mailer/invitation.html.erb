<%= render 'title', title: "#{@recipient.name.try(:+, ' ')}You’re invited!" %>

<p>
  <%= image_tag(@sender.image) %>
  <%= @custom_text %>
</p>


<p>
  Click here to vote and see the results:
  <%= render 'click_here',
    event: @event,
    email: @recipient.email,
    source: 'event-invitation',
    medium: 'email',
    campaign: 'sched.do'
  %>
</p>

<p>
  <%= render 'footer' %>
</p>
