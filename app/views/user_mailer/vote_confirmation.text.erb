Thanks for helping <%= @event.owner.name %> with “<%= @event.name %>”.

You voted for:

<% @event.user_votes(@user).each do |vote| %>
  "<%= vote.suggestion.primary %><%= "," if vote.suggestion.secondary.present? %> <%= vote.suggestion.secondary %>"
<% end %>

To change your vote or find out how other people have voted, visit <%= event_url(@event) %>.

*Did you know you can send your own polls for free? Use sched.do to plan events easily and collect feedback fast on virtually any topic. Learn more at https://www.sched.do.
